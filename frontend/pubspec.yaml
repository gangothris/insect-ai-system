name: frontend
description: Insect Detection App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.1.0'

dependencies:
  flutter:
    sdk: flutter

  image_picker: ^1.0.7
  http: ^1.2.0 
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true